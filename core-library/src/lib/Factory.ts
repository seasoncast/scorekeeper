// // Loads and creates new instance of SportEvent, and Player
// async function createPlayer(params: type) {}
// async function loadPlayer(params: type) {}

// async function createEvent(params: type);
// async function loadEvent(params: type) {}
